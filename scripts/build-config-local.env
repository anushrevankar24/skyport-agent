# SkyPort Build Configuration
# These values will be compiled into the binary during build
# Change these and rebuild to update the production URLs

SKYPORT_SERVER_URL="http://localhost:8080/api/v1"
SKYPORT_WEB_URL="http://localhost:3000"
SKYPORT_TUNNEL_DOMAIN="localhost:8080"
DEBUG_MODE="true"
